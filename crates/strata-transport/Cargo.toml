[package]
name = "strata-transport"
version = "0.1.0"
edition = "2021"
description = "Pure Rust implementation of RIST SIMPLE profile (RFC 4588 / VSF TR-06-2)"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
bytes = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
rand = { workspace = true }

[dev-dependencies]
tracing-subscriber = { workspace = true }
