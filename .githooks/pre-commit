#!/usr/bin/env bash
set -euo pipefail

# Pre-commit: fast checks only. Must not slow you down.
# Clippy is a superset of `cargo check`, so we only run clippy.

echo "[pre-commit] Formatting..."
cargo fmt --all

echo "[pre-commit] Clippy..."
cargo clippy --workspace --all-targets -- -D warnings

exit 0
